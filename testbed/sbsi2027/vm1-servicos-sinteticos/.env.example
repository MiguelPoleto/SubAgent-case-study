# Use digests ou tags previamente validadas e registre-os na planilha do experimento.
NGINX_IMAGE=nginx:1.28-alpine
MAILPIT_IMAGE=axllent/mailpit:latest

# A VM 2 deve alcançar estas portas; limite o acesso por origem com UFW na VM 1.
WEB_PORT=8080
SMTP_PORT=1025
MAILPIT_UI_PORT=8025
